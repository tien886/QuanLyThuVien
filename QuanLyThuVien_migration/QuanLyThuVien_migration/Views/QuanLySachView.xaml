<UserControl x:Class="QuanLyThuVien.Views.QuanLySachView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyThuVien.Views"
             mc:Ignorable="d" 
             Padding="0,32"
             d:DesignHeight="1080" d:DesignWidth="1660">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox 
        Grid.Row="0"
        Style="{StaticResource SearchTextBox}"
        Text="{Binding SearchBarText, UpdateSourceTrigger=PropertyChanged}"
        />
        <DataGrid 
          Grid.Row="1"
          ItemsSource="{Binding Books}"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          CanUserDeleteRows="False"
          IsReadOnly="True"
          Margin="0,32,0,0"
          Style="{StaticResource RoundedDataGrid}">

            <DataGridTextColumn Header="Mã đầu sách" Binding="{Binding Code}" Width="140"/>
            <DataGridTextColumn Header="Tên sách" Binding="{Binding Title}" Width="260"/>
            <DataGridTextColumn Header="Tác giả" Binding="{Binding Author}" Width="180"/>
            <DataGridTextColumn Header="ISBN" Binding="{Binding Isbn}" Width="180"/>
            <DataGridTextColumn Header="Thể loại" Binding="{Binding Category}" Width="150"/>
            <DataGridTextColumn Header="Tổng bản sao" Binding="{Binding TotalCopies}" Width="130"/>
            <DataGridTemplateColumn Header="Có sẵn" Width="100">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Available}"
                   HorizontalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Black}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Available}" Value="0">
                                            <Setter Property="Foreground" Value="{StaticResource Danger/Overdue}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTemplateColumn Header="Thao tác" Width="140">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                            <Button Style="{StaticResource IconButton}"
                  Command="{Binding DataContext.ViewBookCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                <fa:IconImage Icon="Eye" Width="18" Height="18" Foreground="{StaticResource Black}"/>
                            </Button>
                            <Button Style="{StaticResource IconButton}"
                  Command="{Binding DataContext.EditBookCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                <fa:IconImage Icon="PenToSquare" Width="18" Height="18" Foreground="{StaticResource Black}"/>
                            </Button>
                            <Button Style="{StaticResource IconButton}"
                  Command="{Binding DataContext.DeleteBookCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                <fa:IconImage Icon="TrashCan" Width="18" Height="18" Foreground="{StaticResource Black}"/>
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>

        </DataGrid>
    </Grid>
    
</UserControl>
