<UserControl x:Class="QuanLyThuVien.Views.MuonTraSachView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyThuVien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1660">
    <!--
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">

        <StackPanel Orientation="Vertical" Margin="0,0,10,0">

            <UniformGrid Columns="3" Margin="0,0,-20,0">

                <Border Style="{StaticResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Đang mượn" Foreground="{StaticResource TitleOfWindow}" FontSize="16"/>
                            <TextBlock Text="{Binding DangMuon}" FontWeight="Bold" FontSize="24" Margin="0,5,0,0" Foreground="{StaticResource Black}"/>
                        </StackPanel>
                        <Border Grid.Column="1" Width="48" Height="48" Background="{StaticResource InUse_light}" CornerRadius="12"
                                HorizontalAlignment="Right" VerticalAlignment="Center">
                            <fa:IconImage Icon="BookOpen" Foreground="{StaticResource InUse}" Width="24" Height="24" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Style="{StaticResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Quá hạn" Foreground="{StaticResource TitleOfWindow}" FontSize="16"/>
                            <TextBlock Text="{Binding QuaHan}" FontWeight="Bold" FontSize="24" Margin="0,5,0,0" Foreground="{StaticResource Black}"/>
                        </StackPanel>
                        <Border Grid.Column="1" Width="48" Height="48" Background="{StaticResource Danger/Overdue_light}" CornerRadius="12"
                                HorizontalAlignment="Right" VerticalAlignment="Center">
                            <fa:IconImage Icon="TriangleExclamation" Foreground="{StaticResource Danger/Overdue}" Width="24" Height="24" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Style="{StaticResource StatCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Đã trả (tháng này)" Foreground="{StaticResource TitleOfWindow}" FontSize="16"/>
                            <TextBlock Text="{Binding DaTraThangNay}" FontWeight="Bold" FontSize="24" Margin="0,5,0,0" Foreground="{StaticResource Black}"/>
                        </StackPanel>
                        <Border Grid.Column="1" Width="48" Height="48" Background="{StaticResource Success/1_light}" CornerRadius="12"
                                HorizontalAlignment="Right" VerticalAlignment="Center">
                            <fa:IconImage Icon="Check" Foreground="{StaticResource Success/1}" Width="24" Height="24" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

            </UniformGrid>

            <Border Style="{StaticResource BaseCardStyle}">
                <StackPanel Orientation="Vertical">

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" 
                                    VerticalAlignment="Center" Margin="0,0,-20,0">
                            <TextBlock Style="{StaticResource CardHeader}" Margin="0" Text="Danh sách đang mượn ("/>
                            <TextBlock Style="{StaticResource CardHeader}" Margin="0" Text="{Binding DangMuon}"/>
                            <TextBlock Style="{StaticResource CardHeader}" Margin="0" Text=" cuốn)"/>
                        </StackPanel>

                        <TextBox Grid.Column="1"
                                 Style="{StaticResource SearchTextBox}"
                                 HorizontalAlignment="Right">
                            <TextBox.Text>
                                <Binding Path="SearchBarText" UpdateSourceTrigger="PropertyChanged"/>
                            </TextBox.Text>
                        </TextBox>
                    </Grid>

                    <DataGrid ItemsSource="{Binding LoanList}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserResizeRows="False"
                              IsReadOnly="True"
                              MinHeight="400" 
                              Margin="0,20,0,0"
                              Style="{StaticResource RoundedDataGrid}">

                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Mã phiếu" Width="0.8*"
                                                Binding="{Binding LoanID}"
                                                ElementStyle="{StaticResource CellTextCenter}"/>

                            <DataGridTemplateColumn Header="Sinh viên" Width="1.5*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding Student.StudentName}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Sách" Width="2*"
                                                Binding="{Binding BookCopy.Book.Title}"
                                                ElementStyle="{StaticResource CellTextCenter}"/>

                            <DataGridTextColumn Header="Ngày mượn" Width="1*"
                                                Binding="{Binding LoanDate, StringFormat={}{0:dd/MM/yyyy}}"
                                                ElementStyle="{StaticResource CellTextCenter}"/>

                            <DataGridTextColumn Header="Hạn trả" Width="1*"
                                                Binding="{Binding DueDate, StringFormat={}{0:dd/MM/yyyy}}"
                                                ElementStyle="{StaticResource CellTextCenter}"/>

                            <DataGridTemplateColumn Header="Trạng thái" Width="1*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border x:Name="StatusBorder" Style="{StaticResource LoanStatusPillBorder}">
                                            <TextBlock Style="{StaticResource LoanStatusPillText}" x:Name="StatusText"
                                                       Text="{Binding LoanStatusDescription}"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="Thao tác" Width="1*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Nhận trả" 
                                                Style="{StaticResource ReturnButton}">
                                            <Button.Command>
                                                <Binding Path="DataContext.ReturnBookCommand" RelativeSource="{RelativeSource AncestorType=DataGrid}"/>
                                            </Button.Command>
                                            <Button.CommandParameter>
                                                <Binding Path="."/>
                                            </Button.CommandParameter>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>

                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
    -->
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,-20,20">
            <Border Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Đang mượn" Foreground="{StaticResource TitleOfWindow}" FontSize="16"/>
                        <TextBlock Text="{Binding DangMuon}" FontWeight="Bold" FontSize="24" Margin="0,5,0,0" Foreground="{StaticResource Black}"/>
                    </StackPanel>
                    <Border Grid.Column="1" Width="48" Height="48" Background="{StaticResource InUse_light}" CornerRadius="12"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <fa:IconImage Icon="BookOpen" Foreground="{StaticResource InUse}" Width="24" Height="24" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Quá hạn" Foreground="{StaticResource TitleOfWindow}" FontSize="16"/>
                        <TextBlock Text="{Binding QuaHan}" FontWeight="Bold" FontSize="24" Margin="0,5,0,0" Foreground="{StaticResource Black}"/>
                    </StackPanel>
                    <Border Grid.Column="1" Width="48" Height="48" Background="{StaticResource Danger/Overdue_light}" CornerRadius="12"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <fa:IconImage Icon="TriangleExclamation" Foreground="{StaticResource Danger/Overdue}" Width="24" Height="24" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource StatCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Đã trả (tháng này)" Foreground="{StaticResource TitleOfWindow}" FontSize="16"/>
                        <TextBlock Text="{Binding DaTraThangNay}" FontWeight="Bold" FontSize="24" Margin="0,5,0,0" Foreground="{StaticResource Black}"/>
                    </StackPanel>
                    <Border Grid.Column="1" Width="48" Height="48" Background="{StaticResource Success/1_light}" CornerRadius="12"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                        <fa:IconImage Icon="Check" Foreground="{StaticResource Success/1}" Width="24" Height="24" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource CardHeader}" Margin="0" Text="Danh sách phiếu mượn"/>
                <TextBlock Style="{StaticResource CardHeader}" Margin="5,0,0,0" Foreground="{StaticResource TitleOfWindow}" FontWeight="Normal" 
                           Text="(Chỉ hiển thị phiếu đang mượn)"/>
            </StackPanel>

            <TextBox Grid.Column="1" Style="{StaticResource SearchTextBox}"
                     Tag="Tìm kiếm theo Mã phiếu, Sinh viên, Sách...">
                <TextBox.Text>
                    <Binding Path="SearchText" UpdateSourceTrigger="PropertyChanged"/>
                </TextBox.Text>
            </TextBox>
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding LoanList}"
                  Style="{StaticResource RoundedDataGrid}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã phiếu" Binding="{Binding LoanID}" Width="80" ElementStyle="{StaticResource CellTextCenter}"/>

                <DataGridTextColumn Header="Sinh viên" Binding="{Binding StudentName}" Width="1.5*" ElementStyle="{StaticResource CellTextLeft}"/>

                <DataGridTextColumn Header="Sách" Binding="{Binding BookTitle}" Width="2*" ElementStyle="{StaticResource CellTextLeft}"/>

                <DataGridTextColumn Header="Ngày mượn" Binding="{Binding LoanDate, StringFormat=dd/MM/yyyy}" Width="120" ElementStyle="{StaticResource CellTextCenter}"/>

                <DataGridTextColumn Header="Hạn trả" Binding="{Binding DueDate, StringFormat=dd/MM/yyyy}" Width="120" ElementStyle="{StaticResource CellTextCenter}"/>

                <DataGridTemplateColumn Header="Trạng thái" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border x:Name="StatusBorder"
                                Style="{StaticResource LoanStatusPillBorder}">
                                <TextBlock x:Name="StatusText"
                                    Style="{StaticResource LoanStatusPillText}" Text="{Binding LoanStatusDescription}"/>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding LoanStatusDescription}" Value="Đang mượn">
                                    <Setter TargetName="StatusBorder" Property="Background" Value="#E8F0FE"/>
                                    <Setter TargetName="StatusBorder" Property="BorderBrush" Value="#1A73E8"/>
                                    <Setter TargetName="StatusText" Property="Foreground" Value="#1A73E8"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding LoanStatusDescription}" Value="Quá hạn">
                                    <Setter TargetName="StatusBorder" Property="Background" Value="#F7E8E9"/>
                                    <Setter TargetName="StatusBorder" Property="BorderBrush" Value="#EF4444"/>
                                    <Setter TargetName="StatusText" Property="Foreground" Value="#EF4444"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Thao tác" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource ReturnButton}" 
                                    Content="Nhận trả"
                                    Command="{Binding DataContext.OpenNhanTraSachPopupCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Style="{StaticResource PaginationBorder}" Margin="0,20,0,0">
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Stretch" Orientation="Horizontal">
                <Button Command="{Binding GoToPreviousPageCommand}" Content="← Trang trước" Style="{StaticResource BackForwardButtonStyle}" />

                <Border Width="1" Margin="4,0" Background="#E0E0E0" />

                <RadioButton Command="{Binding PageSelectionCommand}" CommandParameter="{Binding FirstPage}" Content="{Binding FirstPage}" Style="{StaticResource PageButtonUnSelectedStyle}" GroupName="PagingGroup" />
                <RadioButton Command="{Binding PageSelectionCommand}" CommandParameter="{Binding SecondPage}" Content="{Binding SecondPage}" Style="{StaticResource PageButtonUnSelectedStyle}" GroupName="PagingGroup" />
                <RadioButton Command="{Binding PageSelectionCommand}" CommandParameter="{Binding ThirdPage}" Content="{Binding ThirdPage}" Style="{StaticResource PageButtonUnSelectedStyle}" GroupName="PagingGroup" />
                <RadioButton Command="{Binding PageSelectionCommand}" CommandParameter="{Binding FourthPage}" Content="{Binding FourthPage}" Style="{StaticResource PageButtonUnSelectedStyle}" GroupName="PagingGroup" />
                <RadioButton Command="{Binding PageSelectionCommand}" CommandParameter="{Binding FifthPage}" Content="{Binding FifthPage}" Style="{StaticResource PageButtonUnSelectedStyle}" GroupName="PagingGroup" />

                <Border Width="1" Margin="4,0" Background="#E0E0E0" />

                <Button Command="{Binding GoToNextPageCommand}" Content="Trang sau →" Style="{StaticResource BackForwardButtonStyle}" />
            </StackPanel>
        </Border>

    </Grid>
</UserControl>