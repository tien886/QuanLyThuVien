<UserControl x:Class="QuanLyThuVien.Views.StudentDetailPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyThuVien.Views"
             xmlns:converters="clr-namespace:QuanLyThuVien.Resources.Converters"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="600">

    <Border Background="{StaticResource White}" CornerRadius="12" Width="600">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2" Color="Gray"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Thông tin sinh viên" 
                               FontSize="20" FontWeight="Bold" Foreground="{StaticResource Black}"/>
                    <TextBlock Text="Chi tiết thông tin tài khoản sinh viên" 
                               FontSize="14" Foreground="{StaticResource TitleOfWindow}" Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource CircularIconButton}"
                        VerticalAlignment="Top"
                        Command="{Binding ClosePopupCommand}">
                    <fa:IconImage Icon="Xmark" Width="16" Height="16" Foreground="{StaticResource TitleOfWindow}"/>
                </Button>
            </Grid>

            <Border Grid.Row="1" Background="{StaticResource Stroke}" Height="1" Margin="-30,0,-30,25"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Mã sinh viên" Style="{StaticResource DetailLabel}"/>
                            <TextBlock Text="{Binding StudentId}" Style="{StaticResource DetailValue}" FontWeight="SemiBold"/>

                            <TextBlock Text="Họ và tên" Style="{StaticResource DetailLabel}"/>
                            <TextBlock Text="{Binding StudentName}" Style="{StaticResource DetailValue}"/>

                            <TextBlock Text="Email" Style="{StaticResource DetailLabel}"/>
                            <TextBlock Text="{Binding Email}" Style="{StaticResource DetailValue}"/>

                            <TextBlock Text="Số điện thoại" Style="{StaticResource DetailLabel}"/>
                            <TextBlock Text="{Binding PhoneNumber}" Style="{StaticResource DetailValue}" Margin="0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Khoa" Style="{StaticResource DetailLabel}"/>
                            <TextBlock Text="{Binding FacultyName}" Style="{StaticResource DetailValue}"/>

                            <TextBlock Text="Ngày tạo" Style="{StaticResource DetailLabel}"/>
                            <TextBlock Text="{Binding RegistrationDate, StringFormat='dd/MM/yyyy'}" Style="{StaticResource DetailValue}"/>

                            <TextBlock Text="Trạng thái" Style="{StaticResource DetailLabel}"/>
                            <Border HorizontalAlignment="Left" Margin="0,0,0,20">
                                <Border Style="{StaticResource StatusPillBorder}">
                                    <TextBlock Style="{StaticResource StatusPillText}"
                                               Text="{Binding AccountStatus, Converter={StaticResource StatusToTextConverter}}"/>
                                </Border>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Border Background="{StaticResource Stroke}" Height="1" Margin="0,10,0,20" Opacity="0.5"/>

                    <TextBlock Text="Thống kê mượn sách" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

                    <UniformGrid Columns="3" Rows="1">
                        <Border Margin="0,0,10,0" Padding="15" CornerRadius="8" Background="{StaticResource DeepBackgroundSolid}">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Tổng đã mượn" Foreground="{StaticResource TitleOfWindow}" FontSize="13" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding TongDaMuon}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource Black}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border Margin="5,0,5,0" Padding="15" CornerRadius="8" Background="{StaticResource InUse_light}">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Đang mượn" Foreground="{StaticResource InUse}" FontSize="13" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding DangMuon}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource InUse}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border Margin="10,0,0,0" Padding="15" CornerRadius="8" Background="{StaticResource Danger/Overdue_light}">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="Quá hạn" Foreground="{StaticResource Danger/Overdue}" FontSize="13" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding QuaHan}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource Danger/Overdue}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="3" Background="{StaticResource Stroke}" Height="1" Margin="-30,25,-30,20"/>

            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1" 
                        Content="Đóng" 
                        Style="{StaticResource RoundedButton}"
                        Background="White"
                        Foreground="{StaticResource Black}"
                        BorderBrush="{StaticResource Stroke}"
                        Width="100" Height="40"
                        Command="{Binding ClosePopupCommand}"/>
            </Grid>

        </Grid>
    </Border>
</UserControl>