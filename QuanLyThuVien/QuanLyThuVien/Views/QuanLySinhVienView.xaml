<UserControl x:Class="QuanLyThuVien.Views.QuanLySinhVienView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyThuVien.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1660">

    <!-- Resource -->
    <UserControl.Resources>
        <Style x:Key="CellTextCenter" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <!-- Circular Icon Button Style -->
        <Style x:Key="CircularIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root" Background="{TemplateBinding Background}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="{StaticResource InUse_light}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background" Value="{StaticResource Stroke}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Trạng thái sính viên - Border -->
        <Style x:Key="StatusPillBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Background" Value="{StaticResource Danger/Overdue_light}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding AccountStatus}" Value="Active">
                    <Setter Property="Background" Value="{StaticResource Success/1_light}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Trạng thái sinh viên - TextBlock -->
        <Style x:Key="StatusPillText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource Danger/Overdue}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding AccountStatus}" Value="Active">
                    <Setter Property="Foreground" Value="{StaticResource Success/1}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="StudentDataGridHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource White}"/>
            <Setter Property="Foreground" Value="{StaticResource Black}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StudentDataGridStyle" TargetType="DataGrid" BasedOn="{StaticResource RoundedDataGrid}">
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource StudentDataGridHeader}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGrid">
                        <Border x:Name="Root"
                                CornerRadius="10" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="DG_ScrollViewer"
                                          Focusable="false"
                                          CanContentScroll="true">
                                <ScrollViewer.Template>
                                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            <Grid Grid.Row="0"
                                                  x:Name="ColumnHeadersPresenter"
                                                  Background="{StaticResource White}"
                                                  Margin="0,0,0,4">
                                                <DataGridColumnHeadersPresenter/>
                                            </Grid>
                                            <ScrollContentPresenter Grid.Row="1"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollViewer.Template>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <!-- Code -->
    <Grid>
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Thanh tìm kiếm -->
        <TextBox 
            Grid.Row="0"
            Style="{StaticResource SearchTextBox}"
            Text="{Binding SearchBarText, UpdateSourceTrigger = PropertyChanged}"
            Tag="Tìm kiếm theo tên, mã SV, email..."/>

        <!-- Bảng thông tin sinh viên -->
            <DataGrid ItemsSource="{Binding Students}"
              Grid.Row="1"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              CanUserResizeRows="False"
              IsReadOnly="True"
              Margin="0,20,0,0"
              Style="{StaticResource StudentDataGridStyle}">

                <DataGrid.Columns>
                    <!-- Cột mã sinh viên -->
                    <DataGridTextColumn Header="Mã SV" Binding="{Binding StudentId}" Width="150"
                                    ElementStyle="{StaticResource CellTextCenter}"/>

                    <!-- Cột họ tên sinh viên -->
                    <DataGridTextColumn Header="Họ và tên" Binding="{Binding StudentName}" Width="2*"
                                    ElementStyle="{StaticResource CellTextCenter}"/>

                    <!-- Cột email -->
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"
                                    ElementStyle="{StaticResource CellTextCenter}"/>

                    <!-- Cột số điện thoại -->
                    <DataGridTextColumn Header="Số điện thoại" Binding="{Binding PhoneNumber}" Width="120"
                                    ElementStyle="{StaticResource CellTextCenter}"/>

                    <!-- Cột khoa  -->
                    <DataGridTextColumn Header="Khoa" Binding="{Binding Department}" Width="2*"
                                    ElementStyle="{StaticResource CellTextCenter}"/>

                    <!-- Ngày tạo  -->
                    <DataGridTextColumn Header="Ngày tạo"
                                    Binding="{Binding RegistrationDate, StringFormat=\{0:dd/MM/yyyy\}}" 
                                    Width="160"
                                    ElementStyle="{StaticResource CellTextCenter}"/>

                    <DataGridTemplateColumn Header="Trạng thái" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource StatusPillBorder}">
                                    <TextBlock Style="{StaticResource StatusPillText}"
                                           Text="{Binding AccountStatus, Converter={StaticResource StatusToTextConverter}}"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>


                    <DataGridTemplateColumn Header="Thao tác" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >

                                    <Button Style="{StaticResource CircularIconButton}"
                                        Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                        <fa:IconImage Icon="Eye" Width="18" Height="18"
                                                  Foreground="{StaticResource Success/1}"/>
                                    </Button>


                                    <Button Style="{StaticResource CircularIconButton}"
                                        Margin="5,0,0,0">
                                        <fa:IconImage Icon="PenToSquare" Width="16" Height="16"
                                                  Foreground="{StaticResource TitleOfWindow}"/>
                                    </Button>

                                    <Button Style="{StaticResource CircularIconButton}"
                                        Margin="5,0,0,0"
                                        Command="{Binding DataContext.ToggleStatusCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}">
                                        <fa:IconImage Width="18" Height="18"
                                                  Icon="{Binding AccountStatus, Converter={StaticResource StatusToIconConverter}}"
                                                  Foreground="{Binding AccountStatus, Converter={StaticResource StatusToIconColorConverter}}"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        
        <!-- 
        <Border Background="#80000000" 
                Visibility="{Binding IsDetailPopupVisible, 
                Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <Border 
            Background="{StaticResource White}"
            CornerRadius="10" VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Width="400"
            Height="550"
            Visibility="{Binding IsDetailPopupVisible, Converter={StaticResource BooleanToVisibilityConverter}}">

            <Border.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>

            <local:StudentDetailPopupView DataContext="{Binding SelectedStudent}"/>

        </Border>
        -->
    </Grid>
</UserControl>