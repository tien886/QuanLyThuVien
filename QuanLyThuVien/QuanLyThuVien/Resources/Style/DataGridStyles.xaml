<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="CellTextCenter" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <Style x:Key="CellTextLeft" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="Margin" Value="10,0,0,0"/>
    </Style>

    <Style x:Key="StatusPillBorder" TargetType="Border">
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Background" Value="{StaticResource Danger/Overdue_light}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding AccountStatus}" Value="1">
                <Setter Property="Background" Value="{StaticResource Success/1_light}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="LoanStatusPillBorder" TargetType="Border">
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Background" Value="{StaticResource Danger/Overdue_light}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding LoanStatusDescription}" Value="Đang mượn">
                <Setter Property="Background" Value="{StaticResource InUse_light}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding LoanStatusDescription}" Value="returned">
                <Setter Property="Background" Value="{StaticResource Success/1_light}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="StatusPillText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource Danger/Overdue}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding AccountStatus}" Value="1">
                <Setter Property="Foreground" Value="{StaticResource Success/1}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding AccountStatus}" Value="Hoạt động">
                <Setter Property="Foreground" Value="{StaticResource Success/1}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="LoanStatusPillText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource Danger/Overdue}"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding LoanStatusDescription}" Value="Đang mượn">
                <Setter Property="Foreground" Value="{StaticResource InUse}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding LoanStatusDescription}" Value="returned">
                <Setter Property="Foreground" Value="{StaticResource Success/1}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DefaultDataGridHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource White}"/>
        <Setter Property="Foreground" Value="{StaticResource Black}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="RoundedDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource White}"/>
        <Setter Property="Foreground" Value="{StaticResource Black}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="RowHeight" Value="44"/>
        <Setter Property="ColumnHeaderHeight" Value="36"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Stroke}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Stroke}"/>
        <Setter Property="AlternationCount" Value="2"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource DefaultDataGridHeader}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGrid">
                    <Border x:Name="Root" CornerRadius="10"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <Border x:Name="ClipBorder" CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                ClipToBounds="True">
                            <ScrollViewer x:Name="DG_ScrollViewer" Focusable="false" CanContentScroll="true">
                                <ScrollViewer.Template>
                                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>
                                            <Grid Grid.Row="0" x:Name="ColumnHeadersPresenter" 
                                                  Background="{StaticResource White}" Margin="0,0,0,4">
                                                <DataGridColumnHeadersPresenter/>
                                            </Grid>
                                            <ScrollContentPresenter Grid.Row="1"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollViewer.Template>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="RowStyle">
            <Setter.Value>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Stroke}"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Style.Triggers>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter Property="Background" Value="#FAFAFA"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource InUse_light}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>

        <Setter Property="CellStyle">
            <Setter.Value>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="8,0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}"  
                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>